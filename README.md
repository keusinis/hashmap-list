# HashMap

A hash map (separate chaining) storing `string` key-value pairs.

## Build & Run

```bash
# build everything
make -f makefile.txt rebuild

# run demo
make -f makefile.txt demo

# run tests (writes results to log.txt)
make -f makefile.txt test

# clean build artifacts
make -f makefile.txt clean
```
